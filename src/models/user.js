export default class User {
    constructor(username, email, password, cash) {
        this.username = username;
        this.email = email;
        this.password = password;
        this.cash = cash;
    }
}